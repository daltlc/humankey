{
  "name": "humankey-example-basic",
  "private": true,
  "type": "module",
  "scripts": {
    "build:client": "esbuild client.ts --bundle --outfile=public/bundle.js --format=esm",
    "start": "npm run build:client && npx tsx server.ts"
  },
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@simplewebauthn/browser": "^13.3.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.0.0"
  }
}
